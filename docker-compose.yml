services:
  trader:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - trader-data:/app/.trader   # sessions, logs, tmp
    restart: unless-stopped

volumes:
  trader-data:
